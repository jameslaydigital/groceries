# Reference only — the live box sets these as `Environment=` lines in the
# systemd units (see deploy/README.md). Nothing reads this file directly.
BASE_DOMAIN=yourdomain.com
DEFAULT_SUBDOMAIN=home
ACME_EMAIL=you@example.com
LINODE_API_TOKEN=your_linode_dns_api_token
